---
name: prd-tech-review
description: >
  从软件工程角度审查已完成的 PRD。PM 已有的配套产物（UI 原型、UX 流程图、DFD、状态机、业务流程图、时序图、ER 图）有则一并审查，没有不要求。
  三阶段：技术审查 → 交互问答（点选 UI）→ 重生成可开发 PRD 与按需验证图。
  触发：审阅/审查 PRD、检查能否开发、找需求缺口、审查后提问补充、根据审查重生成 PRD。
  运行于 Claude Code / Codex / Grok 等 Agent。
---

# PRD 技术审查与迭代

## 做什么

接收**已完成的 PRD**，执行闭环：

```
阶段 1 技术审查 → 结构化问题报告
阶段 2 交互问答 → 点选 UI 收集 PM 决策（默认立刻弹出，不征求许可）
阶段 3 重生成   → 完整 PRD + 按需验证图（Mermaid 等）
```

**核心分工：PM 做决策，AI 画图。** 图不要求 PM 提供；缺图不定级。AI 从文字推导模型，不确定处变成阶段 2 选择题，确认后阶段 3 生成验证图。

## 输入

| 输入 | 必需？ | 说明 |
|------|--------|------|
| PRD（HTML/MD/粘贴/链接） | **必需** | 没有则向用户索取 |
| UI 原型 / 设计稿、UX/DFD/状态机/流程/时序/ER | 可选 | **有则审；缺图本身不定级** |

## 维度执行语义（强制，禁止简化错）

| 维度 | 有产物 | 无产物 |
|------|--------|--------|
| **D1–D10** | 始终审 PRD 文字 | 始终审 |
| **D11** | 按形态分级审（见 `ui-prototype-framework.md`） | **跳过、不定级**；阶段 3 **不生成**高保真原型 |
| **D12–D17** | 审图 + 与 PRD 一致性 | **不因缺图定级**；**必须**文字模型推导；不确定 → 本维记问题进阶段 2；确定部分 → 阶段 3 生成图 |

详细问题与反模式：`references/review-checklist.md`。各图标准：对应 `references/*-framework.md`。

**归维去重：** 一题只挂一个主维度。规则/异常/歧义 → 优先 D6/D7/D9；仅当不确定点**直接决定验证图结构**时挂 D12–D17。禁止同一事实开两道题。

## 阶段 1：技术审查

1. 完整阅读 PRD；若有产物则完整阅读。
2. **模型推导（无论是否有图）：** 有状态实体 / 核心流程 / 前后端交互 / 多实体关系 / 核心用户任务 → 推导隐含模型。能唯一确定的记为阶段 3 素材；多种合理解读 → 审查问题（归维见上）。
3. 按 D1–D10 始终审；D11 / D12–D17 按上表。
4. 每个问题：定级（阻断/重要/建议）+ 引用原文 + 技术风险 + 具体改法。
5. 输出报告（有 `html-report` skill 则用；否则 HTML 文件或 Markdown）：结论、问题表、详述、维度分、下一步「即将点选」。
6. **报告交付后同一轮进入阶段 2**，不要等用户说「继续」。

**严重程度：** 阻断 = 无法开工；重要 = 不澄清会返工；建议 = 可不进问卷。  
**缺图不参与定级。** 总结论一票否决以 **D1–D10** 为主；细则见 checklist 评分指南。

## 阶段 2：交互问答（强制点选）

**必读：** `references/qa-generation-guide.md`

### 工具探测（立刻 call，先于长文）

1. 有 `request_user_input` → **Codex**，立刻调用  
2. 否则有 `AskUserQuestion` 或 `ask_user_question` → **Claude / Grok**，立刻调用  
3. 其它同构多选题工具 → 立刻调用  
4. 仅不存在或调用失败 → 一句说明后 Markdown 降级  

| YOU MUST | YOU MUST NOT |
|----------|--------------|
| 报告后同一轮 tool call | 问「是否进入问答？」 |
| 首个输出动作是 tool call | 先写 Markdown A/B/C 再等人打字 |
| 收到答案后再下一轮 / 阶段 3 | 未收齐决策就重生成或写代码 |

**题量闸门（强制）：**

| 规则 | 值 |
|------|-----|
| 默认只问 | 阻断 + 重要 |
| 建议级 | 默认不进问卷（可写报告跟进） |
| 硬上限 | **≤ 15 题**；用户要求深挖可 **≤ 25** |
| 超出 | 同维合并；可行业默认的进阶段 3 标 ❓，**禁止静默丢弃**（写入「未澄清项」） |
| 单次题量 | Claude/Grok 1–4；Codex 1–3（prefer 1）；Markdown ≤8 |

推荐项第一；**不要**手写 Other（客户端自动提供）。分组顺序：D1+D2 → D8+D9 → D6+D7 → D3+D4+D10 → D11–D17。

若当前为**只读 Plan** 且无法点选：提示用户进入可交互模式或开启对应 feature，再降级 Markdown。

## 阶段 3：重生成与验证图

**必读：** `references/prd-regeneration-guide.md`  
结构模板：`references/functional-module-framework.md`（11 项；**② 原型为可选**，不阻断 D2）

1. 融合：原始 PRD + 审查报告 + PM 回答 → 完整 PRD。  
2. 修订标记：🆕 新增 / 📝 修订 / ❓ 待确认；未明确处行业默认 + ❓。  
3. 验收标准：业务规则 → 正反向用例；异常 → 边界用例。  
4. **按需**生成验证图（有内容才生成）：状态机 / 流程 / 时序 / ER / DFD / UX；每条边可追溯到原文、PM 答案或 ❓ 默认。  
5. 图自检用对应 framework；图文矛盾以 PM 确认为准改文字。  
6. 版本号：`V{原版本或1}.tech-review.{YYYYMMDD}`，勿写死 V2.0。  
7. 输出：完整 PRD + 图 + 产物清单 + 审查问题对照表（html-report 可选，否则 MD/HTML + Mermaid）。

## 审查原则（精简）

- 引用原文；说明技术风险；给可执行改法。  
- 你是开发，不是校对；假设 PM 已尽力——用「缺失」多于「错误」。  
- 已有产物有则审；没提供不定级、不催补图。

## 配套文件

| 文件 | 阶段 |
|------|------|
| `references/review-checklist.md` | 1：17 维问题、反模式、评分与结论 |
| `references/functional-module-framework.md` | 1+3：模块完整性（② 可选） |
| `references/ui-prototype-framework.md` | 1：D11 形态分级（有则审） |
| `references/ux-review-framework.md` 等 6 个图框架 | 1 有则审 + 3 生成自检 |
| `references/qa-generation-guide.md` | 2：点选、去重、题量、模板 |
| `references/prd-regeneration-guide.md` | 3：结构、修订、图、自检 |
| `references/eval-notes.md` | 回归自检（可选） |

## 关联 Skill（均可选）

| Skill | 关系 |
|-------|------|
| `html-report` | 报告/重生成 HTML；**缺失则**直接写 HTML 文件或 Markdown |
| `doc-writing-guide` | 文档写作方法论（若存在） |
