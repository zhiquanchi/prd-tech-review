---
name: prd-tech-review
description: >
  从软件工程角度审查已完成的 PRD 及配套产物（UI 原型、UX 流程图、DFD 数据流图、状态机图、业务流程图、时序图、ER 实体关系图）。
  审查完成后进入交互式问答阶段，针对每个审查问题向产品经理生成选择题，引导 PM 深入思考需求细节。
  最后根据 PM 的回答重新生成一份完整的、开发可直接使用的 PRD。整个流程分为三个阶段：
  技术审查 → 交互问答 → PRD 重生成。
---

# PRD 技术审查与迭代

## 这个 Skill 做什么

这个 Skill 接收一份**已完成的 PRD**——产品经理认为已经写好的——然后执行**三阶段闭环流程**：

```
阶段 1：技术审查    → 站在开发者角度审查 PRD，输出结构化问题报告
阶段 2：交互问答    → 针对每个审查问题，向 PM 生成选择题，引导 PM 深入思考
阶段 3：PRD 重生成  → 根据 PM 回答，重新生成一份开发可直接使用的完整 PRD
```

**为什么要这三步？** 产品经理写完 PRD 后，往往认为需求已经清楚。但站在技术角度，很多细节、边界、异常并未覆盖。通过审查发现问题 → 提问引导思考 → 重生成消除缺口，经过这一轮迭代，PM 对自己的 PRD 会有更深入的理解，开发拿到的是一份经过技术校验的、可直接开工的文档。

## 何时使用

当用户提出以下需求时触发本 Skill：

- 审阅 PRD / 审查产品需求文档 / review PRD
- 检查 PRD 是否可以开始开发
- 找出需求文档中的缺口或风险
- 对产品需求做技术审查
- 审查 PRD 后帮忙问问题 / 引导产品补充细节
- 根据审查结果重新生成 PRD

**输入要求：**

1. **PRD 文档**（HTML 文件、Markdown、粘贴的文本、或文档链接）。
2. **UI 原型**（基于 Ant Design 的可交互原型）——产品使用 Ant Design 框架编写的界面原型，包括 `.tsx`/`.jsx` 源码文件、CodeSandbox/Stackblitz 在线链接、或导出的 HTML。原型是审查的必需输入，PRD 中的交互行为必须与原型一致。
3. **UX 用户流程图**——用户体验流程图、用户旅程图、或信息架构图。展示用户完成核心任务的路径和交互过程。
4. **DFD 数据流图**——数据流图，展示系统中数据的来源、加工、存储和去向。至少包含 Context 图或 Level 0 图。
5. **状态机图**——有状态实体（订单、优惠券、任务等）的状态转换图，展示状态、事件、守卫条件和动作。
6. **业务流程图**——核心业务流程的处理逻辑图（Flowchart），展示处理步骤、决策分支、异常路径。至少覆盖每个核心业务场景（如"领券→用券→下单"、"退款审批"）。
7. **时序图**——核心交互场景的 UML 时序图（Sequence Diagram），展示前端、后端服务、外部系统之间的调用顺序、消息传递和事务边界。至少覆盖：领券、用券、订单取消/退款退券、支付回调。
8. **ER 实体关系图**——数据实体之间的关系图（Entity-Relationship Diagram），展示实体间的关联关系（1:1、1:N、N:M）、主键外键和 cardinality。对应 PRD 数据实体表的可视化表达。

如果没有提供 PRD、UI 原型、UX 流程图、DFD 图、状态机图、业务流程图、时序图或 ER 图，请向用户索取。八者都是审查输入——PRD 描述"做什么"，原型展示"怎么交互"，UX 图展示"用户体验路径"，DFD 图展示"数据怎么流"，状态机图展示"状态怎么变"，流程图展示"逻辑怎么走"，时序图展示"谁先调谁"，ER 图展示"数据怎么关联"。缺失任何一项，审查就不完整。

## 工作流程（三阶段）

### 阶段 1：技术审查

```
步骤 1：完整阅读 PRD 文档
步骤 2：完整阅读 UI 原型（Ant Design 源码 / 在线链接 / 导出 HTML），理解页面结构和交互行为
步骤 3：完整阅读 UX 用户流程图，理解用户路径和交互过程
步骤 4：完整阅读 DFD 数据流图，理解数据流向和加工过程
步骤 5：完整阅读状态机图，理解实体生命周期和状态转换
步骤 6：完整阅读业务流程图，理解处理逻辑和决策分支
步骤 6a：完整阅读时序图，理解系统组件之间的调用顺序和事务边界
步骤 6b：完整阅读 ER 实体关系图，理解数据实体之间的关联关系
步骤 7：按 17 个维度逐项审查（详见 references/review-checklist.md）
        - 维度 D1-D10 审查 PRD 文档
        - 维度 D11 审查 UI 原型与 PRD 的一致性及 Ant Design 使用规范
        - 维度 D12 审查 UX 用户流程图的完整性和可用性
        - 维度 D13 审查 DFD 数据流图的规范性和完整性
        - 维度 D14 审查状态机图的正确性和与业务规则的一致性
        - 维度 D15 审查业务流程图的逻辑完整性和分支完备性
        - 维度 D16 审查时序图的完整性和时序正确性
        - 维度 D17 审查 ER 实体关系图的完整性和规范正确性
步骤 8：对发现的每个问题，定级并定位到 PRD、原型或图表中的精确位置
步骤 9：生成结构化审查报告（通过 html-report skill 输出 HTML）
步骤 10：交付报告——包含总结论（可开发 / 需修改 / 阻断）
```

### 阶段 2：交互式问答

审查报告交付后，**自动进入交互式问答阶段**。针对审查中发现的每个"阻断"和"重要"问题，生成对应的选择题，引导 PM 逐组作答。详细指南见 `references/qa-generation-guide.md`。

#### 强制规则：必须使用 AskUserQuestion

阶段 2 **必须**通过 Claude Code 内置工具 **`AskUserQuestion`** 弹出可点选的选择题 UI（与 Plan 模式同类选择框）。

1. **禁止**用纯 Markdown 列表（A/B/C）或纯文字让 PM 打字回复代替选择框。
2. **禁止**在未收到 `AskUserQuestion` 返回结果前进入阶段 3 或写代码。
3. 每题 2–4 个互斥选项；推荐项放第一并在 label/description 中标明；取舍写在选项 description。
4. 系统通常自带 “Other” 自定义输入，无需在选项里再塞“其他”除非需要强调。
5. 单轮调用 `AskUserQuestion` 时题量建议 ≤ 4（避免一次表单过长）；多题可分多次调用。
6. 若当前环境暂无 `AskUserQuestion`（非 Claude Code / 工具不可用），才降级为 Markdown 选择题，并明确告知 PM。

```
步骤 11：将审查问题转化为选择题（每个阻断/重要问题 → 一道选择题）
         - 每题提供 2-4 个具体选项
         - 每题标注技术团队的推荐选项
         - 每题包含技术说明，解释为什么需要这个信息
步骤 12：按维度分组，分轮提问（每轮用 AskUserQuestion 弹出选择框）
         - 第 1 轮：D1 范围 + D2 功能完整性（先确认"做什么"）
         - 第 2 轮：D8 数据模型 + D9 业务规则（数据和规则是骨架）
         - 第 3 轮：D6 边界与异常 + D7 歧义（明确边界）
         - 第 4 轮：D3 非功能性 + D4 可行性 + D10 风险（约束和风险）
         - 第 5 轮：D11-D17 配套产物（原型/图表相关决策）
         - 每轮不超过 4 题（AskUserQuestion 单次调用），告知 PM 进度
步骤 13：调用 AskUserQuestion → 等待 PM 点选 → 记录每个回答
步骤 14：所有轮次完成后，整理回答记录表
```

**问答原则：**

- **走选择框，不走纯文字。** 一律 `AskUserQuestion`，出现 Plan 模式同类 UI。
- **选项即决策。** 每个选项代表一个不同的产品设计决策，不是对错之分，而是取舍之分。
- **有推荐。** 技术团队基于工程考量推荐的选项排第一位并标注 ✅。
- **含取舍说明。** 每个选项标注它意味着什么（开发成本、用户体验、灵活性等）。
- **场景化提问。** 不问"你的数据模型是什么"——问"优惠券系统需要记录哪些信息？以下是技术团队建议的方案，你倾向哪个？"
- **控制节奏。** 单轮不超过 4 题，避免 PM 疲劳。阻断级问题优先问完。

### 阶段 3：PRD 重生成

PM 回答收集完成后，**自动进入 PRD 重生成阶段**。将原始 PRD + 审查报告 + PM 回答三者融合，生成一份开发可直接使用的完整 PRD。详细指南见 `references/prd-regeneration-guide.md`。

```
步骤 15：以功能模块框架 11 项标准为结构模板
步骤 16：将原始 PRD 内容填入对应章节（保留原始意图）
步骤 17：将 PM 回答填入对应的缺失章节，标注修订来源
         - 🆕 新增：原始 PRD 中完全没有的内容
         - 📝 修订：基于原始内容修改的内容
         - ❓ 待确认：PM 回答不明确，需再次确认
步骤 18：对 PM 未明确回答的部分，填入行业默认方案并标注
步骤 19：自动推导验收标准（业务规则 → 验收用例，异常场景 → 边界用例）
步骤 20：生成配套产物清单（标注哪些需 PM 补充、哪些由技术团队生成）
步骤 21：生成审查问题对照表（确保每个问题都有对应解决）
步骤 22：执行质量自检（10 项检查清单）
步骤 23：通过 html-report skill 输出完整 PRD（HTML 格式，含修订标记高亮）
```

**重生成原则：**

- **保留原始意图。** 原始 PRD 的业务背景和功能描述保留原文，只在缺失处补充。
- **标注修订来源。** 每处修订都标注来源（PM 回答编号或技术默认值）。
- **完整性兜底。** PM 未明确回答的，填入行业默认方案 + ❓ 待确认标记。
- **验收标准自动推导。** 每条业务规则 → 正向 + 反向验收；每个异常场景 → 边界验收。

## 审查维度（17 个方向）

前 10 个维度聚焦 PRD 文档本身。D11-D17 分别聚焦 UI 原型、UX 流程图、DFD 数据流图、状态机图、业务流程图、时序图和 ER 实体关系图。每个维度的详细审查问题和常见反模式见 `references/review-checklist.md`，各维度的详细标准分别见对应的 framework 文件。

### D1 范围清晰度

"做"和"不做"的边界是否明确？标记模糊用词："等"、"类似"、"参考现有功能"、"以及其他"。每个 Out-of-Scope 项必须写明原因，不能只贴个标签。

### D2 功能完整性

每个功能模块是否都有必需的章节？对照 `references/functional-module-framework.md` 中的 11 项完整性标准检查。缺少数据实体、业务规则或异常处理 = 阻断。

### D3 非功能性需求可量化性

每个非功能性需求必须有数字。"要快" / "要稳定" / "高并发" 没有具体指标 = 重要。标记任何没有可衡量阈值的定性形容词。

### D4 技术可行性

约束之间是否冲突？排期是否匹配范围？对其他团队/系统的依赖是否有备选方案？

### D5 验收可测性

每条验收标准能否产生明确的通过/不通过结果？"体验要好" / "显示正常" = 重要。每条标准必须有具体、可观测、二元的结果。缺少边界场景测试 = 重要。

### D6 边界与异常覆盖

PRD 是否覆盖了：空态、断网、加载中、数据过多、无权限、并发操作、过期数据、非法输入？每个缺失场景 = 重要。异常处理标准见 functional-module-framework 参考。

### D7 歧义检测

是否存在可多重解读的表述？"排序按时间"（哪个时间？正序还是倒序？）、"支持多种支付方式"（哪几种？）、"展示相关信息"（什么信息？）。每处歧义 = 重要。

### D8 数据模型健全性

所有数据实体及其属性是否都列出了？实体之间的关系是否清晰？功能描述中隐含但未列入实体表的实体 = 重要。UI 中出现但实体表里没有的属性 = 重要。

### D9 业务规则完备性

每条业务规则是否有"违反了会怎样"的后果？有没有 PM 没覆盖的"如果……怎么办"场景？逐个用户操作走查，问"如果 X 会怎样"——每个未覆盖场景 = 重要。

### D10 风险识别

这份 PRD 中哪些地方最可能导致返工、范围蔓延或集成失败？标记未声明的假设、没有时间线的依赖、以及看起来简单但有隐藏复杂度的功能。

### D11 UI 原型与 PRD 一致性

PRD 描述的交互行为是否与 Ant Design 原型一致？原型是否覆盖了 PRD 中提到的所有页面、状态和操作？Ant Design 组件是否被正确使用——没有重复造轮子、没有错误使用组件 API、没有忽略组件的无障碍/国际化能力？详细审查标准见 `references/ui-prototype-framework.md`。

### D12 UX 审阅

PRD 是否提供了 UX 用户流程图/用户旅程图？用户完成核心任务的路径是否最优？交互模式是否一致？错误体验是否完善？详细审查标准见 `references/ux-review-framework.md`。

### D13 DFD 数据流图审阅

PRD 是否提供了 DFD 数据流图？DFD 是否符合规范（无黑洞/白洞/灰洞、无直连、数据流命名正确）？分层图是否平衡？DFD 与 PRD 的功能模块和数据实体是否一致？详细审查标准见 `references/dfd-review-framework.md`。

### D14 状态机图审阅

PRD 是否为有状态实体提供了状态机图？状态定义是否合理（无状态爆炸、无状态属性混淆）？转换是否完整（无死锁、无不可达状态、守卫条件互斥且完整）？状态机与 PRD 业务规则是否一致？详细审查标准见 `references/state-machine-review-framework.md`。

### D15 业务流程图审阅

PRD 是否为每个核心业务流程提供了流程图？处理逻辑是否正确（无死循环、无不可达路径、无逻辑矛盾）？每个决策分支是否穷举了所有可能结果？流程图与 PRD 的功能模块、业务规则及其他图表是否一致？详细审查标准见 `references/flowchart-review-framework.md`。

### D16 时序图审阅

PRD 是否为核心交互流程提供了时序图（Sequence Diagram）？前端、后端、外部系统之间的调用顺序是否正确？事务边界是否标注？异常路径（超时、失败、回滚）是否有时序分支？时序图与 PRD 操作规则表、状态机图、DFD 是否一致？详细审查标准见 `references/sequence-diagram-review-framework.md`。

### D17 ER 实体关系图审阅

PRD 是否提供了 ER 实体关系图？PRD 数据实体表中的所有实体是否在 ER 图中出现？实体之间的关系是否有 cardinality 标注（1:1、1:N、N:M）？N:M 关系是否有中间表？ER 图是否与 PRD 数据实体表、业务规则、DFD 数据存储一致？详细审查标准见 `references/er-diagram-review-framework.md`。

## 严重程度分级

| 级别 | 含义 | 处理方式 |
|------|------|---------|
| **阻断** | 开发无法启动。核心需求缺失、规格矛盾、或完全没有验收标准。 | 必须在任何工作开始前解决。阶段 2 中必须生成对应问题。 |
| **重要** | 不澄清会导致返工、延期或 bug。歧义需求、缺失边界场景、不可测标准。 | 必须在对应模块开发启动前解决。阶段 2 中必须生成对应问题。 |
| **建议** | 澄清更好但不阻断。轻微不一致、风格问题、非关键细节缺失。 | 可在开发过程中解决。阶段 2 中可酌情合并或跳过。 |

## 阶段 1 输出：审查报告结构

输出报告（通过 `html-report` skill 生成 HTML）包含：

1. **审查结论** — 三选一：✅ 可开发 / ⚠️ 需修改 / 🔴 阻断。附带计数：N 个阻断、N 个重要、N 个建议。
2. **问题汇总表** — 所有问题一张表：序号、严重程度、维度、PRD 位置、问题摘要、修改建议。
3. **逐条详述** — 每个问题展开：引用 PRD 原文、从技术角度说明为什么是问题、PM 具体需要补充或修改什么。
4. **维度评分** — 雷达图/柱状图展示 PRD 在 17 个维度上的得分（每维度 0-3 分：0=严重缺失，3=优秀）。
5. **下一步引导** — 报告末尾提示"即将进入交互式问答阶段，将针对以上问题逐一提问"。

## 阶段 2 输出：交互问答

详细指南见 `references/qa-generation-guide.md`。

**主路径（Claude Code）：** 每轮通过 **`AskUserQuestion`** 弹出选择框，勿输出 Markdown A/B/C 让用户打字。

调用时建议字段映射：

| AskUserQuestion 字段 | 填写内容 |
|----------------------|----------|
| `question` | 场景化问题标题（可含简短技术说明/PRD 引用） |
| `options[].label` | 简短选项名；推荐项放第一，可带 ✅ |
| `options[].description` | 取舍说明（开发成本、体验、风险） |
| `multi_select` | 仅当「可多选补充项」类问题时为 true |

**降级路径（无 AskUserQuestion 时）** 才使用以下 Markdown 格式：

```markdown
## 第 X 轮 / 共 Y 轮 — [维度名称]

> **技术说明：** [为什么需要问这组问题，1-2 句话]

### Q1. [问题标题]

[问题背景，引用 PRD 原文]

- A. ✅ 推荐：[选项A] —— [取舍说明]
- B. [选项B] —— [取舍说明]
- C. [选项C] —— [取舍说明]
- D. 其他（请描述）

---
请逐题作答。如果某个选项不完全符合你的想法，选"其他"并描述你的方案。
```

PM 回答收集完成后，整理为结构化回答记录表，作为阶段 3 的输入。

## 阶段 3 输出：重生成的 PRD

详细指南见 `references/prd-regeneration-guide.md`。重生成的 PRD 包含：

1. **完整的 11 项功能模块结构** — 每个模块都有功能说明、原型、布局、流程、规则、数据、实体、业务规则、异常处理
2. **修订标记** — 🆕 新增（绿色）/ 📝 修订（黄色）/ ❓ 待确认（红色）
3. **范围定义** — In Scope + Out of Scope（含原因和是否预留接口）
4. **非功能性需求** — 全部量化
5. **依赖与约束** — 含备选方案
6. **风险与应对** — 来自 PM 回答的策略
7. **验收标准** — 自动推导（业务规则 + 异常场景 → 验收用例）
8. **配套产物清单** — 8 类产物的状态和负责人
9. **审查问题对照表** — 每个审查问题 → PM 回答 → PRD 对应章节

通过 `html-report` skill 输出 HTML 格式。

## 审查原则

- **PRD 和所有配套产物一起审。** PRD 描述"做什么"，原型展示"怎么交互"，UX 图展示"用户体验路径"，DFD 图展示"数据怎么流"，状态机图展示"状态怎么变"，流程图展示"逻辑怎么走"，时序图展示"谁先调谁"，ER 图展示"数据怎么关联"。八者必须一致——发现不一致就是重要问题。
- **引用原文。** 每个问题必须引用 PRD 或原型代码中触发它的原文。不要转述——直接引。
- **说明技术风险。** 不要只说"这里模糊"。要说"这里模糊是因为 X，意味着开发 A 可能实现成 Y，而测试 B 期望是 Z，导致返工。"
- **给出具体修改建议。** "请澄清"没有用。"请明确：按 created_at 字段倒序排列"才有用。
- **你是开发，不是校对。** 不要标语法或风格问题。标那些会让你写错代码或开发中途跑去问 PM 的问题。
- **假设 PM 已经尽力了。** PRD 不是"错的"——是从技术视角"不完整的"。用"缺失"而不是"错误"来表述问题。

## 问答原则

- **必须用 AskUserQuestion 弹出选择框。** 禁止默认用纯文字 A/B/C 代替（无该工具时才降级）。
- **选项即决策，不是对错。** 每个选项代表一个产品设计取舍，PM 选哪个都"对"——关键是明确选了哪个。
- **有技术推荐。** 每题标注技术团队推荐的选项，但最终决策权在 PM。
- **让 PM 思考，不是替 PM 思考。** 问题引导 PM 考虑他没想到的场景，而不是替他做决定。
- **一次不要太多。** 单轮 AskUserQuestion 不超过 4 题，让 PM 有精力认真思考每道题。
- **问题要具体。** 不问"异常怎么处理"——问"用户选了券但下单时券刚好过期，系统应该怎么处理？"

## 重生成原则

- **保留原始意图。** 原始 PRD 的业务背景和功能描述保留原文，只在缺失处补充。
- **标注修订来源。** 每处修订都标注来源（PM 回答编号或技术默认值）。
- **完整性兜底。** PM 未明确回答的，填入行业默认方案 + ❓ 待确认标记。
- **验收标准自动推导。** 每条业务规则 → 正向 + 反向验收；每个异常场景 → 边界验收。
- **配套产物不遗漏。** 即使 PM 表示后续补充，PRD 中也列出需要哪些产物、覆盖什么内容。

## 配套文件

| 文件 | 用途 | 阶段 |
|------|------|------|
| `references/review-checklist.md` | 17 个维度的详细审查问题、常见反模式表和定级标准 | 阶段 1 |
| `references/functional-module-framework.md` | 11 项功能模块完整性标准——审查时拿 PRD 的每个功能模块跟这个标准对照，缺哪项就是阻断 | 阶段 1 + 3 |
| `references/ui-prototype-framework.md` | Ant Design UI 原型审查标准——组件使用规范、状态覆盖要求、PRD 与原型一致性检查清单 | 阶段 1 |
| `references/ux-review-framework.md` | UX 审阅标准——流程图完整性、用户路径效率、交互一致性、无障碍与错误体验 | 阶段 1 |
| `references/dfd-review-framework.md` | DFD 数据流图审阅标准——规范正确性、分层一致性、DFD 与 PRD 一致性 | 阶段 1 |
| `references/state-machine-review-framework.md` | 状态机图审阅标准——状态定义正确性、转换完整性、状态机与业务规则一致性 | 阶段 1 |
| `references/flowchart-review-framework.md` | 业务流程图审阅标准——逻辑正确性、决策分支完备性、流程图与 PRD 及其他图表一致性 | 阶段 1 |
| `references/sequence-diagram-review-framework.md` | 时序图审阅标准——调用顺序正确性、事务边界标注、时序图与 PRD/状态机/DFD 一致性 | 阶段 1 |
| `references/er-diagram-review-framework.md` | ER 实体关系图审阅标准——实体完整性、关系 cardinality、N:M 中间表、与 PRD/DFD 一致性 | 阶段 1 |
| `references/qa-generation-guide.md` | 交互式问答生成指南——如何将审查问题转化为选择题、选项设计原则、分组策略、各维度问题模板 | 阶段 2 |
| `references/prd-regeneration-guide.md` | PRD 重生成指南——如何融合原始 PRD + 审查报告 + PM 回答，生成完整 PRD 的结构模板、修订规则和质量自检 | 阶段 3 |

## 关联 Skill

| Skill | 关系 |
|-------|------|
| `html-report` | 审查报告和重生成的 PRD 的默认输出格式 |
| `doc-writing-guide` | 文档写作方法论的父级 Skill |
